<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.joinair.mapper.orderMapper">

    <select id="getOrderById" parameterType="int" resultType="com.example.joinair.dto.ORDERS">
        SELECT * FROM ORDERS WHERE Orders_Num = #{Orders_Num}
    </select>

    <select id="getAllOrders" resultType="com.example.joinair.dto.ORDERS">
        SELECT * FROM ORDERS
    </select>

    <insert id="addOrder" parameterType="com.example.joinair.dto.ORDERS">
        INSERT INTO ORDERS (Orders_Num, User_Id, Pro_Code, Orders_Total_Price, Orders_State, Orders_Total)
        VALUES (#{Orders_Num}, #{User_Id}, #{Pro_Code}, #{Orders_Total_Price}, #{Orders_State}, #{Orders_Total})
    </insert>

    <update id="updateOrder" parameterType="com.example.joinair.dto.ORDERS">
        UPDATE ORDERS
        SET User_Id = #{User_Id}, Pro_Code = #{Pro_Code}, Orders_Total_Price = #{Orders_Total_Price},
        Orders_State = #{Orders_State}, Orders_Total = #{Orders_Total}
        WHERE Orders_Num = #{Orders_Num}
    </update>

    <delete id="deleteOrder" parameterType="int">
        DELETE FROM ORDERS WHERE Orders_Num = #{Orders_Num}
    </delete>

</mapper>
